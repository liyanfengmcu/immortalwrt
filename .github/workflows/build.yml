# ImmortalWrt 云编译工作流
# 目标设备: 9tripod_x3568-v4 (RK3568)

name: ImmortalWrt Cloud Build

on:
  workflow_dispatch:
    inputs:
      immortalwrt_version:
        description: 'ImmortalWrt 版本分支'
        required: true
        default: 'openwrt-23.05'
        type: choice
        options:
          - openwrt-23.05
          - main
          - openwrt-21.02
      cpu_count:
        description: '编译线程数 (默认使用全部核心)'
        required: false
        default: 'auto'
        type: string
      custom_packages:
        description: '额外安装的软件包 (空格分隔)'
        required: false
        default: 'luci-app-upnp luci-app-ttyd'
        type: string

env:
  IMMORTALWRT_VERSION: ${{ github.event.inputs.immortalwrt_version || 'openwrt-23.05' }}
  TARGET: rockchip
  SUBTARGET: armv8
  DEVICE: 9tripod_x3568-v4
  # 强制使用 Node.js 24 运行 actions (避免 Node.js 20 弃用警告)
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 720
    
    steps:
      - name: 初始化编译环境
        run: |
          sudo apt-get update -y
          sudo apt-get install -y build-essential clang flex g++ gawk \
            gcc-multilib g++-multilib gettext git libncurses5-dev libssl-dev \
            python3-setuptools rsync swig unzip zlib1g-dev file wget \
            python3-dev python3-pyelftools python3-distutils-extra

      - name: 释放磁盘空间
        run: |
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android/sdk
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          df -h

      - name: 克隆 ImmortalWrt 源码
        run: |
          git clone -b $IMMORTALWRT_VERSION --single-branch --depth=1 \
            https://github.com/immortalwrt/immortalwrt.git immortalwrt
          cd immortalwrt
          git log -1

      - name: 更新 Feeds
        run: |
          cd immortalwrt
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 生成配置文件
        run: |
          cd immortalwrt
          cat > .config << EOF
          CONFIG_TARGET_${TARGET}=y
          CONFIG_TARGET_${TARGET}_${SUBTARGET}=y
          CONFIG_TARGET_${TARGET}_${SUBTARGET}_DEVICE_${DEVICE}=y

          # 镜像配置
          CONFIG_TARGET_ROOTFS_SQUASHFS=y
          CONFIG_TARGET_SQUASHFS_BLOCK_SIZE=256
          CONFIG_TARGET_IMAGES_GZIP=y

          # 镜像输出格式
          CONFIG_TARGET_ROOTFS_EXT4FS=y
          CONFIG_TARGET_EXT4_MAXINODE=60000

          # 基础软件包
          CONFIG_PACKAGE_default-settings=y
          CONFIG_PACKAGE_luci=y
          CONFIG_PACKAGE_luci-base=y
          CONFIG_PACKAGE_luci-compat=y
          CONFIG_PACKAGE_luci-lib-base=y
          CONFIG_PACKAGE_luci-lib-nixio=y
          CONFIG_PACKAGE_luci-ssl=y
          CONFIG_PACKAGE_luci-theme-bootstrap=y
          CONFIG_PACKAGE_luci-theme-material=y

          # 中文支持
          CONFIG_PACKAGE_luci-i18n-base-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-firewall-zh-cn=y
          CONFIG_PACKAGE_luci-i18n-opkg-zh-cn=y

          # 常用工具
          CONFIG_PACKAGE_openssh-sftp-server=y
          CONFIG_PACKAGE_htop=y
          CONFIG_PACKAGE_nano=y
          CONFIG_PACKAGE_automount=y
          # CONFIG_PACKAGE_autosamba=y  # 需要额外 feeds 支持
          CONFIG_PACKAGE_ipv6-helper=y
          CONFIG_PACKAGE_wireless-tools=y

          # 网络工具
          CONFIG_PACKAGE_iptables-mod-tproxy=y
          CONFIG_PACKAGE_iptables-mod-extra=y
          CONFIG_PACKAGE_kmod-tun=y
          CONFIG_PACKAGE_kmod-usb-net-rtl8152=y

          EOF

          # 添加用户自定义软件包
          if [ -n "${{ github.event.inputs.custom_packages }}" ]; then
            for pkg in ${{ github.event.inputs.custom_packages }}; do
              echo "CONFIG_PACKAGE_${pkg}=y" >> .config
            done
          fi

      - name: 显示完整配置
        run: |
          cd immortalwrt
          echo "=========================================="
          echo "    完整 .config 配置文件内容"
          echo "=========================================="
          echo ""
          cat .config
          echo ""
          echo "=========================================="
          echo "    配置统计信息"
          echo "=========================================="
          echo "总配置项数量: $(wc -l < .config)"
          echo "启用的软件包数量: $(grep -c "^CONFIG_PACKAGE.*=y" .config || echo 0)"
          echo "禁用的配置项数量: $(grep -c "^# CONFIG_" .config || echo 0)"
          echo ""
          echo "### 配置文件已显示完成 ###"

      - name: 下载依赖包
        run: |
          cd immortalwrt
          make defconfig
          JOBS=$(nproc)
          echo "使用 ${JOBS} 线程下载"
          # 使用简洁模式下载,仅输出错误和摘要
          make download -j${JOBS} 2>&1 | grep -E "^(Downloading|ERROR|FAILED|make\[)" || true
          echo "下载完成"

      - name: 开始编译
        run: |
          cd immortalwrt
          # 减少编译线程数避免内存不足
          JOBS=$(($(nproc)/2))
          if [ $JOBS -lt 1 ]; then JOBS=1; fi
          echo "使用 ${JOBS} 线程编译 (原CPU核心数: $(nproc))"
          
          # 第一次尝试：多线程编译
          echo "=== 第 1 次编译尝试 ==="
          if make -j${JOBS} 2>&1 | tee compile.log; then
            echo "✅ 编译成功完成"
            exit 0
          fi
          
          # 第一次失败，尝试重试
          echo ""
          echo "::warning::第 1 次编译失败，准备重试..."
          echo "=== 失败的软件包 ==="
          grep -E "ERROR:.*failed to build" compile.log || true
          FAILED_PKG=$(grep -oP "package/feeds/[^/]+/\K[^/]+" compile.log | grep "ERROR" -A1 | tail -1 || echo "")
          
          # 第二次尝试：清理后单线程重试
          echo ""
          echo "=== 第 2 次编译尝试（单线程） ==="
          if [ -n "$FAILED_PKG" ]; then
            echo "清理失败的包: $FAILED_PKG"
            make package/$FAILED_PKG/clean V=s || true
          fi
          
          # 清理编译缓存并重试
          make clean || true
          
          if make -j1 V=s 2>&1 | tee -a compile.log; then
            echo "✅ 第 2 次编译成功"
            exit 0
          fi
          
          # 第二次也失败，输出详细错误
          echo ""
          echo "::error::编译最终失败"
          echo ""
          echo "=== 详细错误信息 ==="
          grep -B5 -A10 "error:" compile.log | tail -50 || true
          echo ""
          echo "=== 失败的软件包 ==="
          grep -E "ERROR:.*failed to build" compile.log || true
          exit 1

      - name: 整理固件文件
        run: |
          cd immortalwrt
          mkdir -p firmware
          
          # 复制并解压镜像文件
          for img_gz in bin/targets/${TARGET}/${SUBTARGET}/*.img.gz; do
            if [ -f "$img_gz" ]; then
              echo "解压: $(basename $img_gz)"
              gunzip -c "$img_gz" > "firmware/$(basename ${img_gz%.gz})"
            fi
          done
          
          # 复制未压缩的镜像文件（如果有）
          find bin/targets/${TARGET}/${SUBTARGET} -maxdepth 1 -type f -name "*.img" -exec cp {} firmware/ \;
          
          # 复制 config.buildinfo (编译配置信息)
          find bin/targets/${TARGET}/${SUBTARGET} -maxdepth 1 -type f -name "config.buildinfo" -exec cp {} firmware/ \;
          
          cd firmware
          # 生成 SHA256 校验文件
          sha256sum * > sha256sums.txt
          
          echo "## 编译信息" >> $GITHUB_STEP_SUMMARY
          echo "- **设备**: ${DEVICE}" >> $GITHUB_STEP_SUMMARY
          echo "- **版本**: ${IMMORTALWRT_VERSION}" >> $GITHUB_STEP_SUMMARY
          echo "- **编译时间**: $(date '+%Y-%m-%d %H:%M:%S')" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 固件文件列表" >> $GITHUB_STEP_SUMMARY
          ls -lh >> $GITHUB_STEP_SUMMARY

      - name: 发布 Release
        if: github.event_name == 'workflow_dispatch'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ env.IMMORTALWRT_VERSION }}-${{ github.run_number }}
          name: ImmortalWrt ${{ env.DEVICE }} - ${{ env.IMMORTALWRT_VERSION }}
          body: |
            ## 编译信息
            - **设备型号**: ${{ env.DEVICE }}
            - **ImmortalWrt 版本**: ${{ env.IMMORTALWRT_VERSION }}
            - **编译时间**: $(date -u '+%Y-%m-%d %H:%M:%S UTC')
            - **额外软件包**: ${{ github.event.inputs.custom_packages }}
            
            ## 固件说明
            下载后请校验 SHA256 值
          files: |
            immortalwrt/firmware/*.img
            immortalwrt/firmware/config.buildinfo
            immortalwrt/firmware/sha256sums.txt
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
